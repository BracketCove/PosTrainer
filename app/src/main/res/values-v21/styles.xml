<resources>

    <style name="AppTheme" parent="AppTheme.Base">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/auto_transition</item>
        <item name="android:windowExitTransition">@transition/auto_transition</item>

    </style>

    <style name="Custom_Checkbox_Overlay" parent="ThemeOverlay.AppCompat">
        <item name="android:colorAccent">@color/WHITE</item>
    </style>

    <style name="Custom_Picker_Text_Color" parent="AppTheme">
        <item name="android:textColorPrimary">@color/BLACK</item>
    </style>
</resources>
